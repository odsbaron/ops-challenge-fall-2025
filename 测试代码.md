python localTest.py --entry_point ops_rolling_regbeta --input_path ./testcase/data_for_rolling_regbeta.parquet --ref_ans_path ./testcase/rolling_regbeta_v1.npy --window 20

git config user.name "odsbaron"
git config user.email "871031393@qq.com"

git add src/solution.py

git commit -m "Accelerate rolling_regbeta operator 测试1"

git push -u origin week2_ods1